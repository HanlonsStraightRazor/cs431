BEGIN
  globalVarInt : INT;
  globalVarReal : REAL;

  INT MethodDec(ArgReal : REAL, ArgInt : INT ) {
    globalVarInt := ArgInt;
    globalVarReal := ArgReal;
  }

  VOID MAIN() {
    mainVarDec : INT;
    mainVarDec := 4;
    MethodDec(6.2, 3);
 }
END
