BEGIN
  globalVarInt : INT;
  globalVarFloat : FLOAT;

  INT MethodDec(ArgFloat : FLOAT, ArgInt : INT ) {
    globalVarInt := ArgInt;
    globalVarFloat := ArgFloat;
  }

  VOID MAIN() {
    mainVarDec : INT;
    mainVarDec := 4;
    MethodDec(6.2, 3);
 }
END
